# Models

Place one or more local models in this directory. The application scans this
directory and its subdirectories for files ending in `.gguf`.

Model files are intentionally excluded from Git because they are large and may
have separate licenses. Restart the app or use **Scan model directory** after
adding a model.
