{
  "name": "portable-local-ai",
  "private": true,
  "version": "1.2.0",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=11.21.0"
  },
  "scripts": {
    "dev": "pnpm --dir frontend dev",
    "build": "pnpm --dir frontend build",
    "preview": "pnpm --dir frontend preview",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
